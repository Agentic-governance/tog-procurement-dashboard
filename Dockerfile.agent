FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn httpx

COPY agent_server.py ./

RUN mkdir -p /app/data

ENV AGENT_DB_PATH=/app/data/monitor.db
ENV OPENAI_API_KEY=

EXPOSE 8010

CMD ["python3", "agent_server.py"]
